{
  "branches": ["main"],
  "plugins": [
    "@semantic-release/commit-analyzer",
    "@semantic-release/release-notes-generator",
    [
      "@semantic-release/exec",
      {
        "prepareCmd": "sed -i 's/\"version\": \"[^\"]*\"/\"version\": \"${nextRelease.version}\"/' taxonomy.json && sed -i 's/version = \"[^\"]*\"/version = \"${nextRelease.version}\"/' pyproject.toml && uv run build.py"
      }
    ],
    [
      "@semantic-release/github",
      {
        "assets": [
          {"path": "taxonomy.msgpack.zst", "label": "Taxonomy DB (msgpack+zstd)"},
          {"path": "taxonomy_terms.tar.gz", "label": "Taxonomy Terms (tar.gz)"}
        ]
      }
    ]
  ]
}
